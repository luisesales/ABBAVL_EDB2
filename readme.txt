1) compila com g++ ou clang++ o arquivo principal chamado main.cpp
2) os arquivos de entrada devem estar na mesma pasta do código fonte e devem ser nomeados arquivo1(dados que criam árvore) e arquivo2(comandos)
3) Basta executar o arquivo executavel gerado que ele vai ler os arquivos de forma correta e gerar os resultados no terminal